# Claude Code Notes

Follow `AGENTS.md` for repository workflow.

In short: manage skills in `skills/`, update `managed-skills.txt`, run `scripts/check` and `scripts/install --dry-run`, and do not edit installed skill directories directly.
